name: flutterproject
description: A new brick created with the Mason CLI.

version: 0.1.0+1

environment:
  mason: ^0.1.2

vars:
  name:
    type: string
    description: Project name
    default: new-app
    prompt: Enter flutter project name
